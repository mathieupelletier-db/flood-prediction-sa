# Root requirements for local development / IDE / lint of the notebooks.
# Notebook-task libraries are installed by the bundle in resources/pipeline.yml,
# and the FastAPI app has its own pinned set in src/app/requirements.txt.
databricks-sdk>=0.30.0
mlflow>=2.14.0
h3==4.1.0
rasterio==1.3.10
shapely==2.0.4
requests==2.32.3
